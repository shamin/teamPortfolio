language: node_js
node_js:
  - "node"

services:
  - postgresql

env:
  - DB=postgres
  - DB_URL=postgres://foo:bar@localhost:5432/testing_db

before_script:
  - psql -c "CREATE DATABASE testing_db;" -U postgres
  - psql -c "CREATE USER foo WITH PASSWORD 'bar';" -U postgres
